/**
 * PDF export — Pandoc bridge for workbook PDFs. Phase 3.
 * Prereq: `pandoc` on PATH.
 */
export async function exportToPdf(_runId: string): Promise<Buffer> {
  void _runId;
  throw new Error("Not implemented — Phase 3");
}
