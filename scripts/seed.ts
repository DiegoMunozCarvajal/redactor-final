// Seed is intentionally empty. Templates are created per-project via POST /api/projects.
// Admin can manage templates at /admin/books.
console.log("No seed data to insert. Each project auto-creates its own template.");
